interface ProfileSectionProps {
    section: string;
  }
  
  const ProfileSection =({ section }: ProfileSectionProps) => {/*}
    return (
      <div className="p-4 bg-white rounded shadow-md">
        {section === "overview" && (
          <div>
            <h2 className="text-2xl font-bold">Overview</h2>
            <p>Este es el contenido de la sección Overview.</p>
          </div>
        )}
        {section === "profile" && (
          <div>
            <h2 className="text-2xl font-bold">Profile</h2>
            <p>Este es el contenido de la sección Profile.</p>
          </div>
        )}
        {section === "resume" && (
          <div>
            <h2 className="text-2xl font-bold">Resume/CV</h2>
            <p>Este es el contenido de la sección Resume/CV.</p>
          </div>
        )}
      </div>
    );
    */}

  export default ProfileSection;